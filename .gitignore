# ── sensitive: user-provided originals never leave this machine
*.xlsx
# Business Suite insights exports — per-post reach/views for whole pages
*.csv
.~lock.*
data/
System Design Request.md
# the sample filenames name the sponsor, so they stay local too
tests/samples_local.py

# python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.coverage
htmlcov/
dist/
build/
*.egg-info/

# tooling / os
.idea/
.vscode/
.DS_Store
nohup.out
*.log

# local tool settings
.claude/

# design docs (kept local only)
docs/

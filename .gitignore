# ── sensitive: user-provided originals never leave this machine
*.xlsx
# Business Suite insights exports — per-post reach/views for whole pages
*.csv
.~lock.*
data/
System Design Request.md
# the sample filenames name the sponsor, so they stay local too
tests/samples_local.py
# the history scrub's own leftovers: the replacement map lists every real client
# identifier that was removed, and the backup still contains the unscrubbed
# history. Committing either would put back exactly what the scrub took out.
relay-scrub-replacements.txt
relay-backup.git/

# python
__pycache__/
*.py[cod]
.venv/
venv/
# built by "Start RELAY.bat"; kept apart from .venv so a checkout shared with a
# Linux box does not have the two environments fighting over one directory
.venv-win/
.pytest_cache/
.coverage
htmlcov/
dist/
build/
*.egg-info/

# tooling / os
.idea/
.vscode/
.DS_Store
nohup.out
*.log

# local tool settings
.claude/

# design docs (kept local only)
docs/

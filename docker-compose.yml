services:
  relay:
    build:
      context: .
      args:
        WITH_BROWSER: "1"   # bake in Chromium for the FB/X/IG collectors
    ports:
      - "8501:8501"
    volumes:
      - ./data:/data
    restart: unless-stopped
